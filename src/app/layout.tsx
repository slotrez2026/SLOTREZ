import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Nav from "@/components/Nav";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Slotrez — Book any service. Run any business.",
  description:
    "Slotrez is the universal booking platform for barbers, trainers, cleaners, therapists, and more. Start free.",
  metadataBase: new URL("https://slotrez.com"),
  openGraph: {
    title: "Slotrez",
    description: "Book any service. Run any business.",
    url: "https://slotrez.com",
    siteName: "Slotrez",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Nav />
        {children}
      </body>
    </html>
  );
}
